<p>ユーザー一覧</p>
<% @users.each do |user| %>
  <div class="user-item">
    <img src="<%= "/images/#{user.image}" %>" width="100" height="100">
    <%= link_to(user.name, "/users/#{user.id}") %>
  </div>
<% end %>